import React from 'react';
import MenuPage from './pages/MenuPage';

function App() {
  // This component is now the main entry point.
  // We can add routing (like React Router) here later.
  return (
    <MenuPage />
  );
}

export default App;